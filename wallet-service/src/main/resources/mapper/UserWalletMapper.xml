<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.u.wallet.mapper.UserWalletMapper">

    <!-- 买家扣减冻结金额：增加 frozen_amount >= #{frozenAmount} 防御性约束 -->
    <update id="updateBuyerWallet">
        UPDATE user_wallet
        SET frozen_amount = #{frozenAmount},
            total_expense = #{totalExpense},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND frozen_amount &gt;= #{frozenAmount}
    </update>

    <!-- 卖家加余额 -->
    <update id="updateSellerWallet">
        UPDATE user_wallet
        SET balance = #{balance},
            total_income = #{totalIncome},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
    </update>

    <!-- 充值加余额 -->
    <update id="updateRechargeWallet">
        UPDATE user_wallet
        SET balance = #{after},
            total_income = #{income},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
    </update>

    <!-- 冻结：增加 balance >= #{balanceAfter} 约束，防止余额变负 -->
    <update id="updateFreezeWallet">
        UPDATE user_wallet
        SET balance = #{balanceAfter},
            frozen_amount = #{frozenAfter},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND balance &gt;= #{balanceAfter}
    </update>

    <!-- 解冻：增加 frozen_amount >= #{frozenAfter} 约束 -->
    <update id="updateUnfreezeWallet">
        UPDATE user_wallet
        SET balance = #{balanceAfter},
            frozen_amount = #{frozenAfter},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND frozen_amount &gt;= #{frozenAfter}
    </update>

    <!-- 提现：增加 balance >= #{balance} 约束，防止余额变负 -->
    <update id="updateWithdrawWallet">
        UPDATE user_wallet
        SET balance = #{balance},
            total_expense = #{totalExpense},
            version = version + 1
        WHERE id = #{id}
          AND version = #{version}
          AND balance &gt;= #{balance}
    </update>

</mapper>
